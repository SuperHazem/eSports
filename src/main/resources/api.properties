# API Configuration

# API Credentials
api.key=your_api_key_here

# API Endpoints
api.url=https://nexra.aryahcr.cc/api/chat

# Request Configuration
api.timeout=30000
api.max_retries=3
api.retry_delay=1000

# Fallback Configuration
api.fallback_enabled=false
api.fallback_url=

# Logging Configuration
api.log_responses=true